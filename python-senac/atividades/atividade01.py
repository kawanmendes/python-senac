# Calcula a área de um retângulo a partir da base e altura informadas pelo usuário
b = input("informe a base")
a = input("informe a altura ")

# Converte os valores para float e calcula a área
r = float(b) * float(a)
print("a valor da area é", r)
